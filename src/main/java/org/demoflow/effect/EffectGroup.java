package org.demoflow.effect;

import org.demoflow.RenderContext;
import org.demoflow.View;
import org.demoflow.calculator.CalculationContext;

/**
 * Several effects grouped together.
 * The effect group may have custom parameters that the contained effects may use in their calculations.
 */
public class EffectGroup extends EffectBase<Object> {

    @Override protected void doUpdate(CalculationContext calculationContext) {
        // IMPLEMENT: Implement doUpdate

    }

    @Override protected void doRender(View view, RenderContext renderContext) {
        // IMPLEMENT: Implement doRender

    }

}
